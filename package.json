{
  "scripts": {
    "build": "gitbook build .",
    "serve": "gitbook serve",
    "clean": "rm -rf _book"
  },
  "dependencies": {
    "gitbook-cli": "github:gengjiawen/gitbook-cli",
    "gitbook-plugin-anchors": "^0.7.1"
  }
}
